'use client'
import { useEffect, useState } from 'react'
const themes = [['original', 'Clareza', 'O tom natural do Foco.'], ['night', 'Noite calma', 'Para reduzir o brilho à noite.'], ['lavender', 'Lavanda', 'Um espaço mais suave e contemplativo.']] as const
export function Ajustes() { const [theme, setTheme] = useState('original'); useEffect(() => { const saved = localStorage.getItem('foco-theme') ?? 'original'; setTheme(saved); document.documentElement.dataset.theme = saved }, []); function select(value: string) { setTheme(value); localStorage.setItem('foco-theme', value); document.documentElement.dataset.theme = value } return <><section className="secondary-hero"><p className="eyebrow">Seu espaço</p><h1>Deixe o Foco com a sua cara.</h1><p>As preferências ficam neste dispositivo e podem ser alteradas quando quiser.</p></section><section className="settings-card"><div><p className="eyebrow">Aparência</p><h2>Plano de fundo</h2></div><div className="theme-options">{themes.map(([value, title, description]) => <button key={value} className={theme === value ? `theme-option ${value} selected` : `theme-option ${value}`} onClick={() => select(value)}><span /><strong>{title}</strong><small>{description}</small></button>)}</div></section><section className="settings-card"><div><p className="eyebrow">Assinatura</p><h2>Plano e cobrança</h2></div><p>Quando a cobrança estiver conectada, você poderá gerenciar sua assinatura aqui.</p><a className="button button-outline" href="/planos">Conhecer os planos</a></section></> }
